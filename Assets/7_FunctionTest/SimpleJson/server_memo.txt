서버에서 데이터를 주고받는방식

tcpip : 동기화 게임에서 사용 (c++이라 제일 빠르다)
webserver : 
p2p, udp는 이제 거의 사용 안한다.

json은 키와 값으로 이루어져있다. 전부 다 string
서버에서 다루는 데이터 방식에따라 파싱하는 방법이 다르다.
http => https로 바뀜 (보안때문에)


// 100 best effect pack